/**
 * PayPal configuration.
 *
 * The Client ID is a publishable identifier — PayPal embeds it in the
 * frontend bundle whenever the JS SDK loads, so hardcoding it here is safe.
 * The matching Secret stays server-side (never in this file).
 *
 * Environment: LIVE (production).
 */
export const PAYPAL_CLIENT_ID =
  "ARA5I0pb-Sr8CDj3wiliKf-qILV9wMuX0YRNaBFbBsVld88v2CWs2ILHegOPuLfizo2G-czuNEyHje0L";

export const PAYPAL_CURRENCY = "USD";